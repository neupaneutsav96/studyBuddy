To ingest data

`python ingest.py --device_type mps`

to run in cli

`python run_studybuddy.py --device_type mps`

use history

`python run_studybuddy.py --use_history`

to start api

`python server.py --use_history`
